import 'package:flutter/material.dart';

class NilaiPrestasi extends StatelessWidget {
  const NilaiPrestasi({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
